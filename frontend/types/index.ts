export interface UserProfile {
  name: string;
  subscription: string;
}

export interface PortfolioSnapshot {
  bankroll: number;
  roi: number;
}

export interface PredictionItem {
  title: string;
  value: string;
}
